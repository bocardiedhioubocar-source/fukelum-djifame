{
  "name": "fukelum-djifame",
  "version": "1.0.0",
  "private": true,
  "description": "Application de gestion pour Fukelum Djifame, maison de couture (Dakar, Sénégal).",
  "scripts": {
    "build": "node build/build.mjs",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "lucide-react": "^0.462.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "firebase-tools": "^13.29.1",
    "tailwindcss": "^3.4.14"
  }
}
